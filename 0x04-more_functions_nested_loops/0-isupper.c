#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: input character.
 * Return: 1 if is an uppercase character, 0 in other case.
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
