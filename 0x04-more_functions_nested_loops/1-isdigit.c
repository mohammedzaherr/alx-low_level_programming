#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9).
 * @c: input number.
 * Return: 1 if is a number (0 to 9), 0 otherwise.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
